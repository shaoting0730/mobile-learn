jobs:
 name: Translate README
 on:
  workflow_dispatch: # 手动触发

  call-translate:
    uses: shaoting0730/shaoting0730/.github/workflows/translate-readme.yml@main
    with:
      file: README.md
    secrets:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
